pipeline {
    agent any

    stages {
        stage('Debug') {
            steps {
                echo 'Pipeline is working'
                sh 'node -v'
                sh 'npm -v'
            }
        }
    }
}
